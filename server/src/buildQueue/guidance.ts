export const PLATFORM_DEV_VALIDATION_GUIDANCE =
  'Validate the changed area before restarting: run `npm run typecheck` and `npm test` scoped to what you changed, then `npm run build`, then `npm run restart`. Run the full test suite separately only to diagnose a failure or when the user or project instructions explicitly require it. Use browser validation only for changes to visible UI, responsive layout, navigation, authentication, or interaction. If live authentication blocks browser validation, use one suitable isolated local environment instead of making repeated live-access attempts. After the restart and a concise health check, finish the build turn so the source queue releases promptly; leave unrelated follow-up work for the next turn.';
